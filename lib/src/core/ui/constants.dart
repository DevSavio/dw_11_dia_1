import 'package:flutter/material.dart';

sealed class Fonts {
  static const fontFamily = 'Poppins';
}

sealed class Constants {
  static const pink = Color.fromARGB(255, 235, 179, 218);
  static const red = Color.fromARGB(255, 247, 65, 65);
  static const deepPurple = Color.fromARGB(255, 219, 80, 219);
  static const blue = Color.fromRGBO(164, 242, 245, 1);
  static const grey = Color(0xFF999999);
  static const greyLight = Color.fromRGBO(211, 212, 212, 1);
}
